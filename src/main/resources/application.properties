# Application Name
spring.application.name=project-allocation-tracker

# Server Port
server.port=8080

# H2 Database Configuration (In-Memory with Dummy Data)
spring.datasource.url=jdbc:h2:mem:allocationdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# H2 Console (Access at http://localhost:8080/h2-console)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Initialize database with dummy data from data.sql
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# Jackson Configuration
spring.jackson.serialization.fail-on-empty-beans=false

# ==========================================
# PostgreSQL Configuration (Uncomment to use)
# ==========================================
#spring.datasource.url=jdbc:postgresql://localhost:5432/primo_skeleton_dev
#spring.datasource.username=primo_dev
#spring.datasource.password=dev_password
#spring.datasource.driver-class-name=org.postgresql.Driver
#spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
#spring.jpa.hibernate.ddl-auto=update

